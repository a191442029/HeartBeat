# 摄像头联动方案评估

> 评估日期：2026-09-20
> 需求：心率报警时，不想再手动打开家里摄像头 App 查看原因，希望摄像头画面/录像与 HRMLink 接收端在同一个 UI 下呈现，并能回看报警前后各 20 秒的视频。

## 1. 结论

**走路线 A：EXE 端原生集成 ONVIF/RTSP**。按四期推进：

1. **期 1**：摄像头 Tab（ONVIF 发现 + 多路子码流实时画面 + 手动切换）
2. **期 2**：多路报警录像（进程内存缓冲，报警时裁剪 ±20 秒存档）+ 时段规则 + 点击回放
3. **期 2.5**：AI 活动标记（事后离线人形检测，排序打标，不做身份承诺）
4. **期 3**：鸿蒙/安卓端远程回放报警视频（HTTP 下载 MP4 + AVPlayer，走已有 Tailscale）
   - **播放器单实例约束（安卓/鸿蒙一致）**：任意时刻只存在一个播放器实例；滑动切换摄像头片段时严格按 `stop → release → Surface 置空 → null` 销毁完成后再创建下一路，绝不重叠；快速连续滑动用状态机防抖（只响应最后停留的一路）。理由：DUOTEL 仅 1GB 内存（实测可用 435MB），播放器是瞬时内存大头（50~80MB），单实例化后峰值恒定
   - 安卓端用应用内 `MediaPlayer` + 硬解（MT8167 的 OMX.MTK VDEC 已实测在位，H.264/H.265 均支持），播放 EXE 的 Tailscale URL，流式播放不落盘；EXE 侧 MP4 端点须支持 Range/206 以支持拖动与快速 prepare

## 2. 路线对比

| 路线 | 做法 | 结论 |
|---|---|---|
| **A. EXE 端原生集成（选定）** | EXE 用 ONVIF 取 RTSP 地址，OpenCV/FFmpeg 解码，Tkinter 新增摄像头面板；报警联动挂现有 `on_alarm_hook` | 一个 UI、无新部署、联动最深；EXE 体积增加约 60MB（OpenCV） |
| B. 外挂中间件（go2rtc / Home Assistant） | NUC 上先转流，EXE 只拉流 | 省解码代码，但多一套部署，Tkinter 嵌网页体验差 |
| C. 鸿蒙端直接拉流 | ArkTS 无原生 RTSP，只能 HLS/MJPEG | 延迟高、稳定性差，且人在家时不需要 |

## 3. 推荐架构

```
家用摄像头 ×N (ONVIF/RTSP)
        │ RTSP 子码流
        ▼
┌─────────────── HRMLink EXE（NUC）───────────────┐
│ ONVIF 服务          WS-Discovery 发现 · GetStreamUri │
│        │                                        │
│        ▼                                        │
│ RTSP 流 → ffmpeg 管道 → 进程内 TS 环形缓冲(~4MB/路)  │  ← 全程不碰 SSD
│        │                                        │
│        ▼                                        │
│ 摄像头面板（新 Tab）   多路画面 · 手动/报警自动切换      │
│        ▲                                        │
│ 报警联动 hook (on_alarm_hook) → 裁剪 ±20s 存档       │
└─────────────────────────────────────────────────┘
        │ captures/ 片段 (MP4 + 封面帧)
        ▼
推送记录（报警行内"视频"按钮 → EXE 内置播放窗）
        │ 期3: HTTP 下载 MP4
        ▼
鸿蒙/安卓接收端（Tailscale）
```

ONVIF 的作用边界：只负责发现设备、枚举 Profiles、拿 `GetStreamUri`（取流地址）；真正的视频走 RTSP。个别品牌取流地址不规范时，留"手动填 rtsp://"兜底。

## 4. 关键设计决策（讨论收敛记录）

### 4.1 报警存档：视频片段，不用截图

- 片段 = **报警前 20 秒**（T+0 即可成片，环形缓冲落盘 + remux，秒级就绪）；不再录报警后固定 20 秒
- 存档同时抓 1 张封面帧做缩略图，推送记录列表好辨认
- 查看：推送记录报警行加"视频"按钮 → EXE 内置播放窗（OpenCV 解码 + Tkinter 播放/暂停/进度条），不跳出主程序
- **取消报警时可"一键存完整片段"**（前 20 秒 → 取消时刻，取自滚动中的环形缓冲），需要细看报警后全程时用，平时零开销

### 4.1a 接收端（安卓/鸿蒙）报警时视频区 UI

- 报警触发：波形区 → 视频面板，出现 **[▶ 回放]** 按钮（不自动播放）；心率数字/BPM/报警色保留在面板上方
- 点回放：播放前 20 秒片段（安卓 MediaPlayer + TextureView 硬解；鸿蒙 AVPlayer + XComponent）
- **播完自动接实时快照**（2fps JPEG 轮询，同一面板内切换，播放器销毁后再切）：观感 = "回放播完接着看现场"；若点回放较晚，存在时间轴跳变（跳过报警后已流逝秒数），可接受
- 取消报警/报警解除：播放器销毁（stop→release→null），波形恢复，面板收起
- 未配置摄像头：一切照旧，面板不出现
- EXE 侧配套：报警 WS 消息附带 `clip_url`（T+0 即有）+ `/camera/snapshot` 快照端点（仅报警期间被轮询）

### 4.2 视频缓冲：进程内 TS 环形缓冲（选定），不写 SSD

用户家里有 UPS，内存缓冲可行。三方案对比后选定：

| 方案 | SSD 写入 | 内存占用 | 代价 |
|---|---|---|---|
| 磁盘分段（ffmpeg segment） | ~5.5GB/天/路（可忽略） | 0 | 无 |
| RAM 盘（ImDisk） | 0 | 固定预留 1~4GB | 需装第三方驱动；重启丢 |
| **进程内环形缓冲（选定）** | **0** | **~4MB/路** | 无第三方依赖；EXE 重启后约 1 分钟重建缓冲 |

实现：ffmpeg 把 RTSP 转成 MPEGTS 裸流经 stdout 管道给 EXE → 内存 deque 环形缓冲（512kbps 下 4MB ≈ 65 秒，留足余量）→ 报警时：缓冲写临时 .ts（2~4MB，唯一碰盘动作）→ 追加实时流 20 秒 → ffmpeg `-c copy` 切 MP4。

UPS 的作用边界：覆盖市电闪断/短时停电；覆盖不了 UPS 耗尽、Windows 重启/蓝屏、EXE 崩溃（丢最近 65 秒，可接受）。注意摄像头自己也需在 UPS 供电范围内，否则停电即断流。

### 4.3 SSD 寿命影响：可忽略（如选持续落盘模式）

- 1 路 512kbps 持续写入 ≈ 5.5GB/天 ≈ 2TB/年；4 路 ≈ 8TB/年
- 消费级 SSD 500GB 盘标称 300 TBW → 4 路全天候也要 ~37 年才到上限
- 与正常 Windows 日常写入（20~50GB/天）同量级
- 分段为顺序大块写入，写放大极小；缓存保留 2 小时自动清理，稳态占用 ~1GB/路

### 4.4 多摄像头归属："报警推哪路"无法自动判断

心率数据与摄像头之间没有位置信息，视频 AI 分不出"谁是谁"（多人在家时尤其如此）。分层解决：

**第一层：多路勾选全存（必做）** —— 每路摄像头一个"报警录像"勾选框，报警时勾选的几路全部裁剪存档，彻底不漏。推送记录按摄像头名称列出：`卧室摄像头 ▶ | 客厅摄像头 ▶`。摄像头命名在设置中可改（"卧室""客厅"）。

**第二层：时段规则（必做，解决 80% 场景）** —— "时段 → 优先摄像头"规则表（如 22:00-7:00 → 卧室）。报警高发在睡眠时段，位置高度可预测，零成本零误判。

**第三层：AI 活动标记（期 2.5 增强）** —— 事后离线抽帧（1 帧/秒 × 40 帧/路）跑人形检测，哪路有人标哪路，仅作"活动标记"辅助翻查，**不承诺是本人**。定位为纯加法的可插拔增强。

**主面板自动弹出**：报警瞬间主面板只弹"默认摄像头"（单独指定），其余路静默存档，避免干扰。

**远期硬件选项（暂不做）**：
- BLE 探头定位（ESPresense 类，ESP32 各房间 RSSI 定位，手环 MAC 已知，MQTT 上报——可复用现有 MQTT 通道）
- 毫米波/PIR 存在传感器（Aqara FP2 类）
- 二者任一落地后，"报警推哪路"从猜测变成确定

### 4.5 Windows 本地 AI 组件选型（期 2.5）

| 方案 | 形态 | 评估 |
|---|---|---|
| **OpenCV DNN + YOLO nano ONNX（选定）** | 模型 6~12MB 打进 EXE | OpenCV 已在依赖清单，零新增运行时；CPU 1 帧/秒足够 |
| ONNX Runtime + DirectML | pip 包，可用 NUC 核显 | 备选 |
| CodeProject.AI Server | 独立 AI 服务 + HTTP API | 现成但多一个常驻服务，与 EXE 便携单体冲突 |
| Windows ML / WinRT | 系统自带 | 对 Python 不友好，放弃 |
| 摄像头端自带人形侦测事件 | AI 在摄像头固件，事件上报 | 零算力首选，支持参差，需实测 |

关键点：**不需要实时分析**——视频已存档，报警后离线检测即可，算力要求大幅下降。

明确不做：摔倒检测（漏报不可接受）、人脸识别（远景小脸识别率低，v2 再议）。

## 5. 挂点与改动位置（EXE 侧）

- 报警联动挂点：`push_notifier.py` 的 `_fire_alarm_sound()` / `on_alarm_hook`（已存在）
- 新增模块：`camera/`（ONVIF 服务、流管理器、环形缓冲、剪辑、AI 标记）
- UI：新 Tab（`UI/` 下新增摄像头面板）；推送记录（`UI/PushHistoryUI.py`）报警行加视频按钮
- 打包：HRMLink.spec 增加 opencv-python、ffmpeg.exe

## 6. 风险清单

| 风险 | 对策 |
|---|---|
| 摄像头子码流默认 H.265，OpenCV 解 HEVC 有兼容问题 | 摄像头后台把子码流改为 H.264 |
| 个别品牌 GetStreamUri 不规范 | 手动填 rtsp:// 兜底 |
| EXE 重启/崩溃丢最近 65 秒缓冲 | 接受（UPS 覆盖主要断电场景），1 分钟内重建 |
| 停电时摄像头断流 | 摄像头纳入 UPS 供电（用户已有 UPS） |
| 打包体积增加 | OpenCV ~60MB + ffmpeg.exe ~80MB，可接受 |
| 多路同时裁剪的磁盘瞬时写入 | 每路 2~4MB，可忽略 |

## 7. 待确认事项

- [ ] 摄像头品牌/型号/数量（ONVIF 兼容性实测：发现、取流、子码流切换）
- [ ] 报警联动默认摄像头与时段规则表的具体配置
- [ ] 片段时长（默认 ±20 秒）与缓冲余量（默认 65 秒）是否可配置化
- [ ] 期 3 鸿蒙端 UI 呈现方式（回放列表 or 报警推送即带视频链接）
