# 心率监测系统架构文档

## 项目概述

本项目是一个基于 Python 的蓝牙心率监测工具，支持通过 BLE 设备获取心率数据，并提供浮动窗口显示、MQTT集成、数据记录等功能。

---

## 项目架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                      主程序入口 (__main__.py)                 │
│                                                              │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │           主窗口 (MainWindow)                         │  │
│  │                                                        │  │
│  │  ┌─────────────┬─────────────┬──────────────────┐  │  │
│  │  │ 设备管理   │ 心率监测   │ MQTT设置       │  │  │
│  │  │             │             │                │  │  │
│  │  │ DevCtrl     │ MonitorUI   │ MQTTSettingsUI │  │  │
│  │  │             │             │                │  │  │
│  │  └──────┬──────┘             └────────┬───────┘  │  │
│  │         │                              │             │  │
│  │  ┌──────▼──────┐              ┌──────▼──────┐  │  │
│  │  │ BLEHeartRate │              │ MQTTClient   │  │  │
│  │  │ Monitor     │              │             │  │  │
│  │  └──────┬──────┘              └──────┬──────┘  │  │
│  │         │                              │             │  │
│  │  ┌──────▼──────┐              ┌──────▼──────┐  │  │
│  │  │ HeartRate   │              │ ConfigFile   │  │  │
│  │  │ Logger     │              │ (config.ini)│  │  │
│  │  └─────────────┘              └──────────────┘  │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

---

主窗口共 9 个 TAB：心率监测 / 设备管理 / 基本设置(含浮动窗口) / MQTT / InfluxDB / Tailscale / 消息推送 / 小爱音箱 / 推送记录

## 目录结构

```
HeartBeat/
├── __main__.py                 # 主程序入口
├── Blegetheartbeat.py          # 蓝牙心率监测核心模块
├── mqtt_client.py             # MQTT客户端模块
├── influxdb_writer.py          # InfluxDB写入模块
├── push_notifier.py            # 多渠道推送(MeoW/Bark/ntfy)
├── irregularity_detector.py    # 心律不齐检测
├── webpush_server.py           # Web推送服务(aiohttp, WS+HTTP轮询)
├── heart_rate_logger.py        # 心率日志记录模块
├── system_utils.py            # 系统工具和配置管理
├── config.ini                 # 配置文件(首次运行自动创建)
├── HRMLink_standalone.spec   # PyInstaller打包配置
├── analyze/                   # 数据分析脚本
│   ├── analyze_heart_rate.py   # 统计分析
│   └── visualize_heart_rate.py # 数据可视化
├── 安卓接收端/                 # HRBubble 安卓工程
├── UI/                       # 用户界面模块
│   ├── __init__.py           # 主窗口和设置界面
│   ├── DevCtrl.py            # 设备管理界面
│   ├── Floatingwin_old.py    # 浮动窗口
│   ├── HeartRateWaveform.py  # 心率波形图组件
│   ├── MQTTSettingUI.py      # MQTT设置界面
│   ├── InfluxDBSettingUI.py  # InfluxDB设置界面
│   ├── TailscaleSettingUI.py # Tailscale设置界面
│   ├── PushSettingUI.py      # 消息推送设置界面
│   ├── PushHistoryUI.py      # 推送记录界面
│   ├── XiaoiSettingUI.py     # 小爱音箱设置界面
│   ├── basicwidgets.py       # 基础UI组件
│   ├── heartratepng.py      # 心率PNG生成
│   └── UpDownloadwin.py     # 更新下载窗口
├── log/                       # 日志目录
│   ├── loger.log            # 系统运行日志
│   └── heart_rate_*.csv     # 心率数据日志
├── dist/                      # 打包输出目录
│   └── HRMLink.exe         # 可执行程序
└── build/                     # 编译临时目录
```

---

## 核心功能模块

### 1. 蓝牙心率监测模块 (Blegetheartbeat.py)

**功能描述**: 通过BLE协议连接心率设备并实时获取心率数据

**核心类**: `BLEHeartRateMonitor`

**主要功能**:
- 扫描BLE设备
- 连接/断开心率设备
- 接收心率通知
- 解析心率数据
- 心率数据统计（最小值、最大值、平均值）

**关键方法**:
```python
async def scan_devices(timeout: float) -> List  # 扫描设备
async def connect_device(device_address: str) -> tuple[bool, str]  # 连接设备
async def disconnect_device()  # 断开连接
def _notification_handler(sender: str, data: bytearray)  # 心率通知处理
def _parse_heart_rate(data: bytearray) -> int  # 解析心率数据
def get_heart_rate_stats() -> Optional[Dict]  # 获取统计数据
```

**数据流**:
```
BLE设备 → 蓝牙通知 → _notification_handler → 解析心率 → 回调函数 → UI更新
```

**优化点**:
- 使用 `deque(maxlen=10000)` 限制心率数据最大长度，防止内存溢出
- 完善的异常处理和资源清理

---

### 2. MQTT客户端模块 (mqtt_client.py)

**功能描述**: 通过MQTT协议将心率数据发送到Home Assistant等智能家居平台

**核心类**: `MQTTClient`

**主要功能**:
- 连接/断开MQTT服务器
- 发布心率数据
- Home Assistant自动发现
- 自动重连机制
- 在线/离线状态管理

**关键方法**:
```python
def connect(config=None) -> bool  # 连接MQTT服务器
def disconnect()  # 断开连接
def on_connect(client, userdata, flags, rc)  # 连接成功回调
def on_disconnect(client, userdata, rc)  # 断开连接回调
def send_discovery_config() -> bool  # 发送HA自动发现配置
def publish_heart_rate(heart_rate, timestamp, status) -> bool  # 发布心率数据
def reconnect()  # 重新连接
```

**MQTT主题结构**:
```
状态主题: homeassistant/sensor/heartrate/state
发现主题: homeassistant/sensor/heartrate/config
可用性主题: homeassistant/sensor/heartrate/availability
```

**优化点**:
- 重连线程超时等待最多5秒，确保资源正确释放
- 完善的异常处理和日志记录

---

### 3. 心率日志记录模块 (heart_rate_logger.py)

**功能描述**: 自动记录心率数据到CSV文件，按天分割日志

**核心类**: `HeartRateLogger`

**主要功能**:
- 按天创建日志文件
- 批量写入心率数据
- 自动切换日期文件
- 获取今日统计数据

**关键方法**:
```python
def log_heart_rate(heart_rate: int, timestamp: str = None)  # 记录心率
def _flush_buffer()  # 批量写入缓冲区
def close()  # 关闭日志文件
def get_today_stats() -> Optional[Dict]  # 获取今日统计
```

**日志文件格式**:
```csv
时间戳,心率值
2025-01-30 10:30:15,75
2025-01-30 10:30:16,76
2025-01-30 10:30:17,75
```

**优化点**:
- 添加缓冲机制，每10条数据写入一次磁盘
- 减少90%的磁盘IO操作，提升性能

---

### 4. 系统工具模块 (system_utils.py)

**功能描述**: 提供系统级工具函数，包括配置管理、日志、启动项管理等

**主要功能**:
- 配置文件读写
- 日志系统
- 进程检测
- 开机自启动管理
- 更新检查
- 依赖包自动安装

**关键函数**:
```python
def init_config()  # 初始化配置
def gs(section, option, default, type_)  # 获取配置
def ups(section, option, value)  # 更新配置
def getlogger()  # 获取日志记录器
def check_run()  # 检查程序是否运行
def add_to_startup()  # 添加开机自启
def remove_from_startup()  # 移除开机自启
def checkupdate()  # 检查更新
```

**配置文件结构** (config.ini):
```ini
[GUI]
use_bg=False
startup=False
update_check=False

[FloatingWindow]
text_color=4292502628
text_base=心率: {rate}
bg_opacity=125
font-size=30

[Device]
last_selected_device={"name":"设备名","address":"xx:xx:xx:xx:xx:xx"}
auto_connect=False
auto_reconnect=True
favorite_devices=[]

[MQTT]
enabled=False
broker=localhost
port=1883
username=
password=
topic=homeassistant/sensor/heartrate/state
discovery_enabled=True
```

---

### 5. 用户界面模块 (UI/)

#### 5.1 主窗口 (UI/__init__.py)

**核心类**: `MainWindow`

**主要功能**:
- 设备管理界面
- 浮动窗口设置
- MQTT设置
- 基本设置
- 系统托盘图标
- 窗口关闭管理

**关键方法**:
```python
def setup_ui()  # 设置UI布局
def setup_connections()  # 建立信号连接
def connect_mqtt()  # 连接MQTT
def on_heart_rate_updated(heart_rate)  # 心率更新处理
def start_mqtt_timer()  # 启动MQTT定时发送
def close_application()  # 关闭应用
```

**优化点**:
- 更新检查线程管理，防止重复创建
- 完善的资源清理逻辑

---

#### 5.2 设备管理界面 (UI/DevCtrl.py)

**核心类**: `DeviceConnectionUI`

**主要功能**:
- BLE设备扫描
- 设备连接/断开
- 设备收藏管理
- 自动连接/重连
- 心率数据显示
- 心率波形图
- 数据导出

**关键方法**:
```python
async def scan_devices()  # 扫描设备
async def connect_device()  # 连接设备
async def disconnect_device()  # 断开设备
def on_heart_rate_update(timestamp, heart_rate)  # 心率更新
def add_to_favorites()  # 添加收藏
def remove_from_favorites()  # 移除收藏
def save_data()  # 保存数据到文件
```

**优化点**:
- 限制心率显示文本最大500行，自动删除旧数据
- 完善的异常处理和日志记录

---

#### 5.3 浮动窗口 (UI/Floatingwin_old.py)

**核心类**: `FloatingHeartRateWindow`

**主要功能**:
- 实时心率显示
- 背景颜色动态变化
- 窗口拖动
- 鼠标穿透
- OBS捕获支持

**关键方法**:
```python
def update_heart_rate(rate=None)  # 更新心率显示
def update_style()  # 更新UI样式
def set_text_color(color)  # 设置文字颜色
def set_bg_opacity(opacity)  # 设置背景透明度
def set_bg_brightness(brightness)  # 设置背景亮度
def set_bg_saturation(saturation)  # 设置背景纯度
```

**优化点**:
- 改进关闭事件处理，添加配置项控制关闭行为

---

#### 5.4 心率波形图 (UI/HeartRateWaveform.py)

**核心类**: `HeartRateWaveform`

**主要功能**:
- 实时波形显示
- 心率区间背景色
- 平均值线
- 统计信息显示
- 自动缩放Y轴

**关键方法**:
```python
def add_heart_rate(heart_rate)  # 添加心率数据
def update_plot()  # 更新波形图
def clear_data()  # 清除数据
def get_statistics()  # 获取统计信息
```

**优化点**:
- 改进图形元素异常清理，添加日志记录

---

#### 5.5 MQTT设置界面 (UI/MQTTSettingUI.py)

**核心类**: `MQTTSettingsUI`

**主要功能**:
- MQTT服务器配置
- 用户名密码设置
- 主题配置
- Home Assistant自动发现
- 连接测试

**关键方法**:
```python
def setup_ui()  # 设置UI
def load_settings()  # 加载设置
def save_settings()  # 保存设置
def test_connection()  # 测试连接
def get_config()  # 获取配置
```

---

#### 5.6 更新下载窗口 (UI/UpDownloadwin.py)

**核心类**: `UpdWindow`

**主要功能**:
- 新版本下载
- 下载进度显示
- 下载取消
- 自动更新启动

**关键方法**:
```python
def set_url(url, gitcodeurl)  # 设置下载地址
def start_download()  # 开始下载
def update_progress(progress)  # 更新进度
def download_finished(success)  # 下载完成
def cancel_download()  # 取消下载
```

---

## 技术栈

### 核心技术

| 技术 | 版本 | 用途 |
|------|--------|------|
| Python | 3.13+ | 主开发语言 |
| PyQt5 | 5.15+ | GUI框架 |
| asyncio | 内置 | 异步编程 |
| bleak | 2.1+ | BLE蓝牙通信 |
| paho-mqtt | 2.1+ | MQTT协议 |
| matplotlib | 3.10+ | 数据可视化 |
| numpy | 2.3+ | 数值计算 |
| aiohttp | 3.11+ | WS推送/HTTP轮询(安卓接收端) |
| PyInstaller | 6.16+ | 程序打包 |

### 主要依赖库

```
# GUI框架
PyQt5==5.15.11
PyQt5-Qt5==5.15.2
PyQt5-sip==12.18.0
qasync==0.28.0

# 蓝牙通信
bleak==2.1.1
winrt-Windows.Devices.Bluetooth==3.2.1
winrt-Windows.Devices.Bluetooth.Advertisement==3.2.1
winrt-Windows.Devices.Bluetooth.GenericAttributeProfile==3.2.1
winrt-Windows.Devices.Enumeration==3.2.1
winrt-Windows.Foundation==3.2.1
winrt-Windows.Storage.Streams==3.2.1

# MQTT通信
paho-mqtt==2.1.0

# 数据可视化
matplotlib==3.10.8
numpy==2.3.5

# 其他
python-dateutil==2.9.0
pillow==12.1.0
```

---

## 数据流

### 心率数据流

```
BLE设备
    ↓
蓝牙通知 (BLE Notification)
    ↓
_notification_handler (Blegetheartbeat.py)
    ↓
_parse_heart_rate (解析心率值)
    ↓
heart_rate_callback (回调函数)
    ↓
on_heart_rate_updated (UI/__init__.py)
    ├─→ HeartRateLogger (记录到CSV)
    ├─→ MQTTClient (发送到MQTT)
    ├─→ FloatingHeartRateWindow (浮动窗口显示)
    ├─→ InfluxDBWriter (写入InfluxDB)
    ├─→ NotifierManager (多渠道推送)
    ├─→ FloatingHeartRateWindow (浮动窗口显示)
    ├─→ HeartRateWaveform (波形图显示)
    └─→ WebPushServer (WS推送/HTTP轮询)
```

### MQTT数据流

```
心率数据
    ↓
MQTTClient.publish_heart_rate()
    ↓
MQTT Broker
    ↓
Home Assistant / 其他MQTT客户端
    ↓
传感器状态更新
```

---

## 配置管理

### 配置文件位置
- Windows: `config.ini` (与EXE同目录)
- 开发环境: 项目根目录下

### 配置项说明

#### GUI配置
| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|----------|------|
| use_bg | bool | False | 允许后台运行 |
| startup | bool | False | 开机自启动 |
| update_check | bool | False | 启动时检查更新 |

#### 浮动窗口配置
| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|----------|------|
| text_color | int | 4292502628 | 文字颜色 |
| text_base | str | "心率: {rate}" | 文字模板 |
| bg_opacity | int | 125 | 背景透明度 |
| bg_brightness | int | 90 | 背景亮度 |
| bg_saturation | int | 165 | 背景纯度 |
| font-size | int | 30 | 字体大小 |
| padding | int | 10 | 内边距 |
| register_as_window | bool | False | 注册为常规窗口 |

#### 设备配置
| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|----------|------|
| last_selected_device | JSON | None | 最后选择的设备 |
| auto_connect | bool | False | 自动连接 |
| auto_reconnect | bool | True | 自动重连 |
| favorite_devices | JSON列表 | [] | 收藏的设备 |

#### MQTT配置
| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|----------|------|
| enabled | bool | False | 启用MQTT |
| broker | str | localhost | 服务器地址 |
| port | int | 1883 | 端口号 |
| username | str | "" | 用户名 |
| password | str | "" | 密码 |
| topic | str | homeassistant/sensor/heartrate/state | 状态主题 |
| discovery_enabled | bool | True | 启用自动发现 |
| discovery_topic | str | homeassistant/sensor/heartrate/config | 发现主题 |

#### InfluxDB配置
| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| enabled | bool | False | 启用InfluxDB |
| url | str | "" | 服务器地址 |
| token | str | "" | 访问令牌(DPAPI加密存储) |
| org | str | "" | 组织名称 |
| bucket | str | "" | 存储桶 |

#### Tailscale配置
| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| enabled | bool | False | 启用Web推送服务 |
| address | str | auto | 绑定地址(auto=自动探测Tailscale IP) |
| port | int | 8765 | 服务端口 |

---

## 日志系统

### 日志文件位置
- 系统日志: `log/loger.log`
- 心率日志: `log/heart_rate_YYYY-MM-DD.csv`

### 日志级别
- DEBUG: 详细调试信息
- INFO: 一般信息
- WARNING: 警告信息
- ERROR: 错误信息

### 日志轮转
- 系统日志最大5MB，保留3个备份
- 心率日志按天分割，自动创建新文件

---

## 部署方式

### 1. 直接运行EXE
- 优点: 门槛低，双击即可运行
- 缺点: 文件较大，不够透明

### 2. Python源码运行
- 优点: 完全透明，可随时修改代码
- 缺点: 需要配置Python环境和依赖

### 3. PyInstaller编译
- 优点: 可自行编译最新版本
- 缺点: 门槛较高

---

## 性能优化

### 已实现的优化

1. **内存管理**
   - 心率数据使用 `deque(maxlen=10000)` 限制最大长度
   - 心率显示文本限制最大500行，自动删除旧数据

2. **磁盘IO优化**
   - 日志写入添加缓冲机制，每10条数据写入一次
   - 减少90%的磁盘IO操作

3. **资源管理**
   - MQTT重连线程超时等待，确保资源正确释放
   - 蓝牙连接异常处理，确保资源清理
   - 更新检查线程管理，防止重复创建

4. **异常处理**
   - 所有关键操作都有完善的异常处理
   - 详细的日志记录，便于问题排查

---

## 安全性

### 数据安全
- MQTT密码/InfluxDB Token 使用 Windows DPAPI 加密存储
- MQTT连接支持TLS加密（可扩展）
- 本地数据存储，不上传云端

### 隐私保护
- 不收集用户数据
- 所有数据本地存储
- 开源代码，可自行审计

---

## 扩展性

### 可扩展的功能

1. **数据导出**
   - 支持CSV格式导出
   - 可扩展支持Excel、JSON等格式

2. **数据可视化**
   - 波形图显示
   - 可扩展支持更多图表类型

3. **第三方集成**
   - MQTT协议支持
   - 可扩展支持HTTP API、WebSocket等

4. **多设备支持**
   - 支持多个心率设备
   - 可扩展支持其他BLE设备

---

## 故障排查

### 常见问题

1. **蓝牙扫描不到设备**
   - 检查蓝牙是否开启
   - 检查设备是否开启心率广播
   - 尝试在系统蓝牙设置页面刷新

2. **MQTT连接失败**
   - 检查服务器地址和端口
   - 检查网络连接
   - 检查用户名密码是否正确

3. **程序无法启动**
   - 检查Python版本是否为3.13+
   - 检查依赖包是否安装完整
   - 查看日志文件获取详细错误信息

4. **数据未记录**
   - 检查log目录是否存在
   - 检查磁盘空间是否充足
   - 检查文件权限

---

## 版本信息

当前版本: v1.0.0

版本号格式: v主版本.次版本.修订版本.构建号

---

## 开发者信息

项目仓库:
- GitHub: https://github.com/a191442029/HeartBeat

---

## 许可证

本项目采用开源许可证，具体请查看项目根目录的LICENSE文件。

---

## 更新日志

### v1.0.0 (2026-09-18)
- 首个公开版本：BLE心率监测、心率波形独立页、浮动窗口
- 数据推送：MQTT(Home Assistant自动发现) / InfluxDB / MeoW·Bark·ntfy 多渠道通知
- Tailscale 内网穿透 + 安卓接收端 HRBubble 悬浮窗
- 敏感配置(MQTT密码/InfluxDB Token) DPAPI 加密存储

---

## 联系方式

如有问题或建议，请通过 GitHub Issues 反馈。

---

*本文档最后更新时间: 2026-09-18*
