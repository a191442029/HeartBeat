# HRMLink v1.1.0 ESP32 中继功能 · 代码审查报告

- 审查日期：2026-09-24
- 审查范围：`relay_hub.py`（中枢）、`esp32/hr_relay/hr_relay.ino` + `config.h`（固件）、`UI/RelaySettingUI.py`、`UI/DevCtrl.py`（中继接入点）、`UI/__init__.py`（钩子注入）、`ESP32/_smoke_hub_test.py`
- 审查维度：潜在 BUG / 健壮性（代码强度）/ 内存 / 线程安全 / 安全性
- 方法：人工逐行通读 + 线程模型推演 + 协议两端交叉比对（中枢发送 vs 固件解析）

## 结论速览

| 编号 | 级别 | 位置 | 问题 | 后果 |
|---|---|---|---|---|
| P0-1 | 严重 | 固件 hr_relay.ino:151 | 协议字段不匹配：固件解析 `"cmd"` 键，中枢全部消息发的是 `"type"` 键 | 真机上 connect/cfg/disconnect/scan 全部被固件忽略，手环永远连不上 |
| P0-2 | 严重 | relay_hub.py:478 | 探测切换迟滞比较方向写反 | 漫游永远切不到更强节点，探测式切换整体失效 |
| P1-1 | 高 | 固件 hr_relay.ino:90-94/206-210 | BLE 连接建立后服务发现失败的路径不清连接（僵尸连接） | 手环被节点"隐形占坑"直到重启，全屋失联 |
| P1-2 | 高 | DevCtrl.py:152 + relay_hub.py:268 | 中继心率回调从 hub 线程直调 Qt 控件（跨线程 GUI） | 使用中继数据时偶发崩溃/渲染异常（v1.1.0 新引入） |
| P1-3 | 高 | 固件 hr_relay.ino:164/327、176/353 | millis() 回绕未用减法比较 | 连续运行 49.7 天后连接超时/探测窗判定失效 |
| P2-1~10 | 中/低 | 见 §4 | 10 项健壮性/一致性改进 | 边界场景劣化 |
| M-1 | 中 | 固件 ScanCb | 扫描回调高频构造 Arduino String | 长期堆碎片风险 |

级别定义：**P0** = 真机联调必然失效；**P1** = 特定条件下崩溃/卡死/资源泄漏；**P2** = 边界健壮性；**M** = 内存专项。

---

## 1. P0-1 协议字段不匹配（真机必现）

**现象**：中枢所有 EXE→节点 消息以 `"type"` 为命令字段（relay_hub.py:237 `cfg`、:398 `connect`、:409 `disconnect`、:419 `scan`），而固件 `handle_cmd()` 第一行就找 `"cmd"` 键：

```cpp
// hr_relay.ino:149-151
void handle_cmd(uint8_t* payload) {
  char val[64];
  if (!json_str((const char*)payload, "cmd", val, sizeof(val))) return;  // 永远匹配不到
```

中枢线上报文是 `{"type": "connect", "mac": "..."}`，其中不存在 `"cmd"` 子串 → `handle_cmd` 直接 return，**所有命令被静默丢弃**。

**影响链**：connect 永不执行 → 中枢 18s 超时判定失败 → fail_count 累加 → 换节点重试 → 再超时，无限循环；`cfg` 的 MAC 下发同样失效。真机烧录后第一台节点就会卡死在"待命不连接"。

**为何冒烟测试没抓到**：`_smoke_hub_test.py` 用 Python 模拟节点，按中枢自己的协议解析消息，固件侧解析逻辑完全没有被覆盖（见 §6 测试缺口）。

**修复**（二选一，必须统一两端）：
- 推荐：固件一行改动，`"cmd"` → `"type"`（节点只会收到 EXE→节点 消息，type 值恰好就是命令名，与节点→EXE 方向的字段名对称，协议更干净）；
- 或中枢改为发送 `{"cmd": "connect", ...}`（改动点多，不推荐）。

---

## 2. P0-2 探测切换迟滞比较方向反转

relay_hub.py:478：

```python
if best_name and self.probe_pre_rssi and best_rssi <= self.probe_pre_rssi - self.hysteresis_db:
    ... # 漫游切换
else:
    ... # 回连原持有者
```

RSSI 是负数，"候选强于原持有者 10dB 以上"应该是 `best_rssi >= probe_pre_rssi + hysteresis_db`（例如持有者 -80，候选须 ≥ -70）。当前条件 `best_rssi <= probe_pre_rssi - hysteresis_db`（候选 ≤ -90）的实际含义是"**候选比原持有者弱 10dB 以上才切换**"——

- 真正更强的候选（如 -55）被拒绝 → 回连原持有者；
- 反而在候选显著更弱时才切换。

**后果**：探测式切换（本次方案的核心漫游机制）几乎永远切不到正确节点，等于整体失效；最坏情况切到更弱节点造成体验倒退。

**修复**（一行）：

```python
if best_name and self.probe_pre_rssi and best_rssi >= self.probe_pre_rssi + self.hysteresis_db:
```

---

## 3. P1 高优先级问题

### P1-1 固件僵尸连接占坑（hr_relay.ino:90-94、206-210）

`ble_connect_band()` 三个失败返回点中，后两个（`getService`/`getCharacteristic` 为空）发生在 **BLE 链路已建立之后**，返回 false 时连接仍然挂着且未订阅：

```cpp
if (!pClient->connect(target_addr)) return false;   // (a) 未连上, 无残留, OK
NimBLERemoteService* svc = pClient->getService(...);
if (!svc) return false;                              // (b) 已连接! 残留
pHrChr = svc->getCharacteristic(...);
if (!pHrChr) return false;                           // (c) 已连接! 残留
```

残留后的连锁反应：
1. loop 判定 `ble_connected == false`（只有订阅成功才置 true）→ 回到扫描；但手环已被连上、停止广播 → `target_seen` 不再置位 → 15s 超时上报 `ble_fail`；
2. 中枢重发 connect → 固件 `if (ble_connected) ble_disconnect()` 因 ble_connected=false 被跳过 → `pClient->connect()` 对已连接客户端再调返回 false → 死循环；
3. **WS 断开的放手规则同样失效**：`case WStype_DISCONNECTED: if (ble_connected) { ble_disconnect(); ... }` 跳过 → 即使中枢/UI 关闭，手环仍被该节点占用。

**后果**：手环被"隐形连接"占住，中枢以为失败、其他节点扫不到广播，全屋失联，只能断电重启节点。

**修复**：

```cpp
// ble_connect_band() 失败路径统一清理
bool ble_connect_band() {
  ble_stop_scan();
  if (!pClient) { ... }
  bool ok = pClient->connect(target_addr);
  if (ok) ok = (svc && pHrChr && pHrChr->subscribe(...));   // 合并三段
  if (!ok) {
    if (pClient->isConnected()) pClient->disconnect();       // 关键: 清残留连接
    return false;
  }
  ble_connected = true;
  return true;
}

// onWsEvent 放手规则覆盖僵尸态
case WStype_DISCONNECTED:
  ws_up = false;
  if (ble_connected || (pClient && pClient->isConnected())) ble_disconnect();
  state = ST_IDLE;
  break;
```

### P1-2 中继心率回调跨线程直调 Qt 控件（v1.1.0 新引入）

直连路径是安全的：项目用 qasync，bleak 通知回调 `_notification_handler`（Blegetheartbeat.py:132-152）执行在 **GUI 线程**，直调控件无问题。

中继路径不安全：`relay_hub` 跑在独立线程的 asyncio loop，`_dispatch` 的 hr 分支（relay_hub.py:268-272）**同步调用** `self._hr_callback` → `DeviceConnectionUI.on_heart_rate_update`（DevCtrl.py:347-351）→ `monitor_ui.append_heart_rate()`（DevCtrl.py:89-102，操作 QTextEdit/QTextCursor + 波形控件）。**非 GUI 线程直接操作 QWidget/QTextDocument 违反 Qt 线程规则**，轻则控制台警告、重则偶发崩溃（QTextDocument 内部状态损坏），且难以稳定复现。

**修复**（DevCtrl.py 注册处，改用信号中转，跨线程 emit 会被 Qt 自动排队到 GUI 线程）：

```python
# 类属性区
hr_from_relay = pyqtSignal(float, int)

# __init__ 注册处替换直接注册
self.hr_from_relay.connect(self.on_heart_rate_update)
relay_hub.set_hr_callback(self.hr_from_relay.emit)
```

注意配套问题：中继回调传的时间戳是 `time.time()`（float epoch），直连传的是 `"%Y-%m-%d %H:%M:%S"` 字符串——`on_heart_rate_update` 需要统一转换（见 P2-2），否则中继心率在日志里显示为 `[1758...98]` 这类 epoch 数字。

### P1-3 固件 millis() 回绕（49.7 天）

```cpp
connect_deadline = millis() + BLE_CONN_TIMEOUT_S * 1000UL;   // :164
if (millis() > connect_deadline) { ... }                      // :327  回绕后长时间恒假
scan_until = millis() + (unsigned long)max(dur, 200L);        // :176
if (scan_until && millis() > scan_until) { ... }              // :353  同上
```

millis() 约 49.7 天回绕。回绕后 `millis()` 变小而 deadline 是巨大值，比较长时间为假：CONNECTING 状态卡死（扫描等待分支不超时）、探测窗到点不回包。同文件的 `last_hr_ms`/`last_hb_ms`/`band_heard_ms` 都已经用了正确的**减法形式**（:344、:360、:368），唯独这两处遗漏。

**修复**（统一减法形式）：

```cpp
unsigned long connect_start = 0;                       // 改记起点
connect_start = millis();                              // :164
if (millis() - connect_start >= BLE_CONN_TIMEOUT_S * 1000UL) { ... }  // :327

unsigned long scan_start = 0; unsigned long scan_dur = 0;
scan_start = millis(); scan_dur = max(dur, 200L);      // :176
if (scan_dur && millis() - scan_start >= scan_dur) { ... }            // :353
```

---

## 4. P2 健壮性改进清单

| 编号 | 位置 | 问题 | 建议 |
|---|---|---|---|
| P2-1 | relay_hub.py:419 / 固件:174 | `json_str()` 只解析带引号的字符串值；中枢发的 `dur:1000` 是数值 → 固件解析失败静默用默认 1000（当前恰好相等未爆雷）；将来 `apply{port:数字}` 同坑 | 中枢把 dur/port 发成字符串，或固件 json_str 增加数值分支 |
| P2-2 | relay_hub.py:270 / DevCtrl.py:347 | 中继时间戳 float epoch 与直连 str 时间戳不一致，日志显示为 epoch 数字 | 中枢侧格式化为 `%Y-%m-%d %H:%M:%S` 再回调，或 on_heart_rate_update 内统一转换 |
| P2-3 | relay_hub.py:218-238 | 同一 WS 连接发两次 hello 且改名时，`_nodes[旧名]` 条目不删除（ws 仍指向新连接）→ 幽灵节点可被 `_best_idle` 选中。当前固件每连接只 hello 一次，实际不触发 | hello 时若该 wsr 已绑定旧名，先 `del self._nodes[旧名]` |
| P2-4 | 固件 json_str:135 | `strstr` 键匹配存在前缀碰撞（找 `"mac"` 会命中 `"macx"`）；当前协议键集合无冲突，属理论风险 | 匹配后校验下一字符是 `":"` |
| P2-5 | RelaySettingUI.py:150-153 | `update_display` 每 3s 在 GUI 线程调 `get_local_ip()`（UDP connect 0.5s 超时）；断网/严格防火墙下 UI 偶发卡顿 | IP 缓存（启动时/保存时刷新一次），或仅在 hub 启动事件时更新 |
| P2-6 | relay_hub.py:120-134 | `stop()` 的 `join(timeout=3)` 超时极端情况下旧线程未死、`_thread` 已置 None → `start()` 启动新线程端口冲突（WinError 10048 只记日志） | join 超时则保留 `_thread` 引用拒绝重启并提示；或 site.start 失败时向 UI 回报明确错误 |
| P2-7 | relay_hub.py:193-196 | token 用普通字符串比较（非常量时间）+ 无失败锁定；消息大小依赖 aiohttp 默认 4MB 上限。局域网威胁模型下可接受 | 可选加固：`hmac.compare_digest`；限制 max_msg_size |
| P2-8 | 固件 start_provision | SoftAP 密码固定 12345678，且 WiFi 连不上时 AP 常开 → 物理邻近者可改节点配置 | 后续版本：配网成功后关 AP，或按键触发配网 |
| P2-9 | relay_hub.py:250 | 节点 hb 上报的 state 直接写入 `node["state"]`，可能与中枢仲裁状态短暂不一致（如 hb 报 active 但 active_node 是别的节点），仅影响 UI 显示 | UI 状态列以 hub 侧 active_node/pending 仲裁结果为准 |
| P2-10 | relay_hub.py:89 | `_warned_no_mac`、probe 状态在 stop/start 之间未复位 | stop() 中一并复位 |

---

## 5. 内存专项

### Python 侧：总体健康
- `heart_rate_data` 用 `deque(maxlen=10000)` 有界 ✔
- 心率日志框 `max_display_lines` 循环清理（DevCtrl.py:93-101，且正确处理单次 append 多行）✔
- `_scan_results` 每轮探测清空 ✔；`_nodes` 无主动上限，但唯一增长点是 P2-3 幽灵条目（当前固件行为不触发）
- `asyncio.run_coroutine_threadsafe` 返回的 Future 被丢弃，但 `_safe_send` 全量吞异常，不会产生 "exception was never retrieved" 噪音 ✔

### 固件侧：一处长周期风险
- **M-1（中）ScanCb 高频 String 分配**（hr_relay.ino:55）：`String s = dev->getAddress().toString().c_str()` 在扫描回调中对**每个广播包**做一次 Arduino String 堆分配。嘈杂 BLE 环境（商场/小区）每秒几十包 × 7×24 运行 → ESP32 堆碎片化，长周期后可能 malloc 失败重启。建议改用栈缓冲 + C 函数比较（NimBLEAddress::toString() 本身返回 std::string，无需 Arduino String）：

  ```cpp
  void onResult(const NimBLEAdvertisedDevice* dev) override {
    if (!band_mac.length()) return;
    std::string s = dev->getAddress().toString();
    if (strcasecmp(s.c_str(), band_mac.c_str()) != 0) return;   // 忽略大小写, 零堆分配
    ...
  }
  ```
- `ws_send buf[256]`、配网页 `buf[1600]`：栈分配 + snprintf 截断，安全 ✔
- `json_str` 输出缓冲有 `(e - p) >= outlen` 越界校验 ✔
- 配网 `/save` 对入参长度无校验，超长值 `prefs.putString` 静默失败（NVS 容量限制）→ 建议校验 ssid≤32、pass≤64、host≤48（P2 级）

---

## 6. 测试覆盖缺口

- `_smoke_hub_test.py` 是**中枢侧自测**（Python 模拟节点），两端协议字段一致性（P0-1）和固件解析逻辑完全未覆盖 → 这是 P0-1 漏网的根因。
- 建议：① 真机联调脚本作为阶段 0 验收必需项（烧录 1 台跑通 hello→connect→ble_up→hr 全链路，中枢节点表应显示"● 持有手环"）；② 修复后把 P0-2 的迟滞场景写成中枢侧单测（模拟 scan 回包 RSSI 断言切换/回连分支）。

---

## 7. 修复优先级建议

1. **真机联调前必须修**：P0-1（固件一行）、P0-2（中枢一行）、P1-1（固件失败路径清理）、P1-2（信号中转）
2. **烧录时顺手修**：P1-3（millis 减法形式）、P2-1（dur 数值）、P2-2（时间戳统一）、M-1（ScanCb 去 String）
3. **阶段 1 打磨**：P2-3 ~ P2-10
4. 修复后重跑 `_smoke_hub_test.py` + 真机联调，再出 v1.1.1 构建
