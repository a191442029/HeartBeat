# 安卓接收端方案 — 心率数据 → Tailscale → Android 8.0 悬浮窗

> 制定日期：2026-09-17
> 需求：Android 8.0 手机通过 Tailscale 获取 EXE 心率数据，以悬浮窗形式显示在其他 App 之上；WebSocket 推送为主、HTTP 轮询兜底；EXE 端新增独立"Tailscale"设置 TAB（地址可编辑）。

---

## 一、总体架构

```
BLE手环 → EXE (PyQt5/qasync)                    Android 8.0 手机
   │  heart_rate_callback 挂点                        │
   ├─► aiohttp 服务 (qasync loop 内, 免新线程)         │
   │    ├─ WS  /ws            ← 推送(主通道)          │  Tailscale VPN
   │    ├─ GET /api/heartrate ← 轮询(兜底通道)   ──────►  (WireGuard隧道)
   │    └─ GET /              ← HTML演示页            │
   └─ 绑定 Tailscale IP 100.x.x.x:8765                ▼
                                              HR APP (前台服务)
                                                ├─ WS客户端, 断线自动降级轮询
                                                └─ 悬浮窗 TYPE_APPLICATION_OVERLAY
```

**接收端定位**：EXE 是唯一数据源（"手机直连手环 BLE"是另一个大模块，本期不做）。

---

## 二、阶段 1：EXE 端

### 2.1 新增 `webpush_server.py`（核心新模块）

| 项 | 设计 |
|---|---|
| 框架 | **aiohttp**（一个依赖同时提供 WS 推送 + HTTP 轮询 + 静态页） |
| 启动方式 | 挂到现有 qasync 事件循环：`loop.create_task(site.start())`，零新线程 |
| `GET /api/heartrate` | 返回 `{"heart_rate":72,"timestamp":"...","status":"connected","device":"HUAWEI Band..."}`，格式与 MQTT payload 保持一致 |
| `WS /ws` | 连上即推当前快照；之后每次心率更新广播；`asyncio.create_task` 包裹 send，慢客户端不阻塞回调；断开自动清理 |
| 绑定地址 | **优先使用用户在"Tailscale"TAB 中手填的地址**；填 auto/留空则自动探测本机 `100.64.0.0/10` 网段 IP；探测不到回退 127.0.0.1 并告警 |
| 状态维护 | 暴露 `update(heart_rate, timestamp, status)` 方法，内部缓存最新值供 HTTP 轮询和 WS 新连接使用 |

### 2.2 新增"Tailscale"设置 TAB（TailscaleSettingUI.py）

- 主窗口独立设置 TAB（现位于消息推送之前），经 `_make_page()` 包装，QFormLayout(标签右对齐) + 限宽 640 + hint_label + 按钮横排左对齐
- 内容：
  - 启用开关
  - **Tailscale 地址（可编辑）**：默认 auto（自动探测 100.x 网段），可手填具体 IP
  - 端口（默认 8765）
  - 只读提示行：显示实际生效的服务地址（如 `ws://100.x.x.x:8765`）
  - "保存设置" / "重载服务"按钮；保存即断开旧服务并按新配置重启
- `update_ui_state` 维护启用联动；保存发出 `tailscale_settings_changed` 信号 → 主窗口重启/停止服务

### 2.3 接线点

- [UI/__init__.py](UI/__init__.py) `on_heart_rate_updated` → 顺带调 `web_server.update(hr, ts, "connected")`；设备断开处同步推 `status="disconnected"`（与 MQTT availability 逻辑对齐）
- 主窗口启动时按配置 start，`close_application` 里 stop
- `__main__.py` 增加 `pip_install_models(import_aiohttp, "aiohttp")` + `distribution('aiohttp')` 依赖检查；aiohttp 纯 Python，PyInstaller 自动收集

### 2.4 配置存储

```ini
[Tailscale]
enabled = True
address = auto     ; auto=自动探测Tailscale IP / 手填IP
port = 8765
```

### 2.5 验证与记录（按项目流程）

1. 改前备份至 `_backup/code_YYYYMMDD_N/`
2. 离屏实例化 9 个 TAB 回归
3. 本机验证：`curl http://127.0.0.1:8765/api/heartrate` + 浏览器开 `/` 页面看实时刷新
4. `优化任务2026年9月16日.md` 顶部追加执行记录

---

## 三、阶段 2：Android 8.0 APP（独立新工程）

### 3.1 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| minSdk / targetSdk | **26 / 28** | 26=安卓8.0 起点；28 避免对悬浮窗/前台服务的额外收紧，侧载无限制 |
| 语言/构建 | Java + Android Studio，Gradle | 依赖最少，老设备兼容性最好 |
| 网络库 | **OkHttp 3.14**（支持 API 21+） | WS 客户端 + HTTP 轮询一个库全包 |
| JSON | org.json（系统内置） | 零依赖 |

### 3.2 四大组件

**① `HeartRateService`（前台服务——保活核心）**
- `startForegroundService()` 启动，5 秒内 `startForeground()`（安卓 8.0 硬性要求）
- 通知渠道 `NotificationChannel`（API 26 起必须），低重要性常驻通知"心率监听运行中"
- 内部持有 WS 连接 + `ScheduledExecutor` 轮询器 + 可选 WakeLock（锁屏收流保险）

**② 数据接收策略（WS 主 + 轮询兜底）**
- WS 连接 `ws://<电脑Tailscale IP>:8765/ws`，指数退避重连 2/5/10/30s
- WS 断开超过 5 秒 → 自动降级为每 2 秒 `GET /api/heartrate` 轮询；WS 恢复即停轮询
- 数据超过 10 秒未更新 → 悬浮窗状态点变灰（设备断连/电脑离线）

**③ `OverlayManager`（悬浮窗）**
- `WindowManager.addView()`，窗口类型 `TYPE_APPLICATION_OVERLAY`（API 26 专用类型）
- Flags：`FLAG_NOT_FOCUSABLE` + `FLAG_NOT_TOUCH_MODAL`（不抢焦点、不挡下层操作）
- 内容：小气泡显示 `❤ 72`，可拖动（onTouch 计算位移），位置存 SharedPreferences 记忆
- 点击气泡切换"展开/迷你"两档大小；状态点颜色：绿=WS 实时 / 黄=轮询中 / 灰=数据超时

**④ `MainActivity`（设置与权限引导）**
- 输入：电脑 Tailscale IP、端口、模式（WS+轮询 / 纯轮询）
- 权限三件套引导：`Settings.canDrawOverlays()` → 跳转 `ACTION_MANAGE_OVERLAY_PERMISSION` 授权页；电池优化白名单（`ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`）；通知权限提示
- 启动/停止按钮 + 实时连接状态显示

### 3.3 Android 8.0 专项处理清单

- 后台不能用 `startService()` → 一律 `startForegroundService()`
- Doze 限制 → 前台服务 + 电池白名单基本免疫；Tailscale 侧需"始终开启的 VPN"
- Tailscale 手机端装 API 26 兼容版本（官方最低恰好支持 8.0）

### 3.4 工程文件清单（约 10 个文件）

```
app/src/main/
  AndroidManifest.xml          # SYSTEM_ALERT_WINDOW, FOREGROUND_SERVICE, 电池优化权限
  java/.../MainActivity.java
  java/.../HeartRateService.java
  java/.../OverlayManager.java
  res/layout/activity_main.xml + overlay_bubble.xml
  build.gradle / settings.gradle
```

---

## 四、联调部署 Checklist

1. EXE 更新后本机 curl 验证轮询接口 + 浏览器验证 WS 页面
2. 手机装 Tailscale → 登录同一 tailnet → `ping` 电脑 100.x IP 通
3. 装 APK → 授权悬浮窗+电池豁免 → 填 IP（或用 EXE 端 TAB 页显示的地址）→ 启动服务
4. **锁屏压测**：手机锁屏、电脑后台运行，观察悬浮窗数据是否持续 → 决定是否开 WakeLock
5. 电脑休眠 = 数据源中断（电源计划需设"不休眠"）

## 五、主要风险

| 风险 | 对策 |
|---|---|
| Tailscale 未来版本放弃 API 26 | 锁定旧版 APK 侧载 |
| aiohttp 新增依赖约 1MB | 打包体积略增，需重新出 EXE |
| WS 在 Doze 深睡下偶发断连 | 自动降级轮询 + 指数退避重连，体验无损 |
| 电脑休眠 | 电源计划设"接通电源时不休眠" |

