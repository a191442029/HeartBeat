# 心率日志记录功能说明

## 功能概述

心率日志记录功能会自动将所有接收到的心率数据保存到CSV文件中，方便后续分析和查看。

## 日志文件位置

日志文件保存在项目的 `log` 目录下，文件名格式为：
```
heart_rate_YYYY-MM-DD.csv
```

例如：
- `heart_rate_2025-10-30.csv` - 2025年10月30日的心率数据
- `heart_rate_2025-10-31.csv` - 2025年10月31日的心率数据

## 日志文件格式

日志文件采用CSV格式，包含以下列：

| 列名 | 说明 | 示例 |
|------|------|------|
| 时间戳 | 记录心率数据的时间 | 2025-10-30 14:23:45 |
| 心率值 | 心率数值（次/分钟） | 72 |

示例内容：
```csv
时间戳,心率值
2025-10-30 14:23:45,72
2025-10-30 14:23:46,73
2025-10-30 14:23:47,71
```

## 特性

1. **自动切换文件**：每天午夜会自动切换到新的日志文件
2. **实时保存**：心率数据会立即写入磁盘，不会因程序崩溃而丢失
3. **追加模式**：如果程序重启，会继续在当天的文件中追加数据，不会覆盖已有数据
4. **UTF-8编码**：使用UTF-8编码，支持中文，可以用Excel或任何文本编辑器打开

## 数据分析

你可以使用以下工具打开和分析CSV日志文件：

1. **Microsoft Excel** - 直接双击打开CSV文件
2. **Python分析脚本** - 项目自带的分析和可视化工具（推荐）
3. **任何文本编辑器** - 查看原始数据

### 使用内置分析工具

项目提供了两个Python脚本用于分析心率数据：

#### 1. 数据统计分析 (analyze_heart_rate.py)

这个脚本可以快速统计和分析心率数据：

```bash
# 分析最新的日志文件
python analyze_heart_rate.py

# 分析指定日期的日志
python analyze_heart_rate.py 2025-10-30

# 列出所有日志文件
python analyze_heart_rate.py --list

# 显示帮助信息
python analyze_heart_rate.py --help
```

**输出示例：**
```
============================================================
文件: heart_rate_2025-10-30.csv
============================================================
数据点数: 3600
最低心率: 65 bpm
最高心率: 145 bpm
平均心率: 82.5 bpm
心率中位数: 80 bpm

心率区间分布:
  静息 (<60)     :      0 (  0.0%) 
  正常 (60-100)  :   3200 ( 88.9%) ████████████████████████████████████████████
  偏高 (100-120) :    350 (  9.7%) ████
  很高 (>120)    :     50 (  1.4%) 
```

#### 2. 数据可视化 (visualize_heart_rate.py)

这个脚本使用matplotlib绘制心率趋势图和分布图：

**安装依赖：**
```bash
pip install matplotlib
```

**使用方法：**
```bash
# 可视化最新的日志文件（组合图表）
python visualize_heart_rate.py

# 可视化指定日期的日志（组合图表）
python visualize_heart_rate.py 2025-10-30

# 仅显示趋势图
python visualize_heart_rate.py -t 2025-10-30

# 仅显示分布图
python visualize_heart_rate.py -d 2025-10-30

# 显示帮助信息
python visualize_heart_rate.py --help
```

**图表内容：**
- **趋势图**：显示心率随时间的变化曲线，包含平均线和正常区间标记
- **分布图**：显示心率值的分布直方图，不同区间用不同颜色区分
- **统计信息**：显示数据点数、时间跨度、最低/最高/平均心率

### 自定义Python分析

如果你想自己编写分析代码，可以使用pandas：

```python
import pandas as pd
import matplotlib.pyplot as plt

# 读取日志文件
df = pd.read_csv('log/heart_rate_2025-10-30.csv')

# 基本统计
print(f"平均心率: {df['心率值'].mean():.1f}")
print(f"最高心率: {df['心率值'].max()}")
print(f"最低心率: {df['心率值'].min()}")

# 绘制心率曲线
plt.figure(figsize=(12, 6))
plt.plot(df['心率值'])
plt.title('心率变化曲线')
plt.xlabel('数据点')
plt.ylabel('心率 (bpm)')
plt.grid(True)
plt.show()
```

## 注意事项

1. 日志文件会随着时间累积，建议定期备份或清理旧文件
2. 如果log目录不存在，程序会自动创建
3. 日志记录功能不影响主程序的性能
4. 即使MQTT功能未启用，心率数据仍会记录到日志文件

## 故障排除

**问题：找不到日志文件**
- 检查程序运行目录下的 `log` 文件夹
- 确保程序有写入权限

**问题：日志文件无法打开**
- 确保程序已关闭，正在运行的程序会锁定文件
- 使用支持UTF-8编码的工具打开

**问题：数据没有记录**
- 检查是否成功连接到心率设备
- 查看程序日志（loger.log）了解详细错误信息

